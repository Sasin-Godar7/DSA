// to perform the linear searching 

#include<stdio.h>
#include<stdlib.h>
int main()
{
    int result , arr[n],n,key;
    printf("enter the size of an array :");
    scanf("%d",n);

    printf("\nenter the elements in an array :");
    for(int i=0;i<=n;i++)
    {
        scanf("%d",&arr[i]);
    }

    printf("\n enter the key value to search :");
    scanf("%d",&key);

}
